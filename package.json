{
  "name": "pi-raft",
  "version": "0.1.0",
  "private": true,
  "description": "Workflow enforcement extension for pi agents operating in slock. Intercepts bash calls, validates workflow state, and blocks violations.",
  "keywords": [
    "pi-package",
    "pi",
    "raft",
    "slock",
    "agent-workflow"
  ],
  "license": "MIT",
  "scripts": {
    "lint": "git diff --check -- . ':!node_modules' && bash -n test-harness/run-scenario.sh && bash -n test-harness/assertions/check.sh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ci": "npm run lint && npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.0",
    "typebox": "^1.2.0"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ],
    "prompts": [
      "./prompts"
    ]
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
